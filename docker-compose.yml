services:
  app:
    container_name: lecentre-app
    build:
      context: .
      target: dev
    volumes:
      - .:/app
      - ./node_modules:/app/node_modules
    working_dir: /app
    restart: unless-stopped
    ports:
      - 5173:5173
